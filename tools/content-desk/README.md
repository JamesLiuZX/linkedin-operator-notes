# Content desk

Local pipeline ledger + evidence gate + live anti-slop scorer.
Mirrors `scripts/content-check.mjs` so the browser and CI agree.

```bash
npm run desk:install
npm run desk
```

Opens at http://localhost:5174. State persists in `localStorage`.
