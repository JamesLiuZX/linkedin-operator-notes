# Autopilot — moved to JamesLiuZX/command

The portfolio queue, doctrine, autopilot loop, human-only blockers, and the
status log now live in the private HQ repo:
**github.com/JamesLiuZX/command** (`AUTOPILOT.md` there).

Read and log there. This stub remains so old links don't dangle. If an
unattended session cannot reach the command repo, note your status line here
and flag the fallback in your summary.
